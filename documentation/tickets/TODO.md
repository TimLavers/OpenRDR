- Remember the user-set width of the chat panel
- Run all tests in github actions for each push
- Write a cuke for the scenario where the user tries to cancel a rule when there is none in progress
- Don't ask for confirmation of a condition if it is exactly the same expression that the user entered
- Provide the facility for the user to ask the bot to create a derived attribute with a specified name where the value
  is generated by the AI according to instructions provided by the user. For example, the user might ask the bot to
  create a derived attribute called "Diabetes status" where the value is determined from the clinical notes in the case.